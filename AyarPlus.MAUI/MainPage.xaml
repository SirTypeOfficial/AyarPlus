<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:skia="clr-namespace:SkiaSharp.Extended.UI.Controls;assembly=SkiaSharp.Extended.UI"
             x:Class="AyarPlus.MAUI.MainPage"
             Title="Lottie Player">

    <Grid RowDefinitions="*,Auto,Auto,Auto" Padding="20">
        
        <skia:SKLottieView x:Name="LottieView"
                           Grid.Row="0"
                           Source="sample.json"
                           RepeatCount="-1"
                           IsAnimationEnabled="True"
                           HorizontalOptions="Fill"
                           VerticalOptions="Fill" />

        <Grid Grid.Row="1" ColumnDefinitions="*,Auto" Margin="0,20,0,10">
            <Slider x:Name="ProgressSlider"
                    Grid.Column="0"
                    Minimum="0"
                    Maximum="1"
                    Value="0"
                    ValueChanged="OnSliderValueChanged"
                    VerticalOptions="Center" />
            
            <Label x:Name="ProgressLabel"
                   Grid.Column="1"
                   Text="0%"
                   WidthRequest="50"
                   HorizontalTextAlignment="End"
                   VerticalOptions="Center"
                   Margin="10,0,0,0" />
        </Grid>

        <HorizontalStackLayout Grid.Row="2" 
                               HorizontalOptions="Center" 
                               Spacing="20"
                               Margin="0,10">
            <Button x:Name="PlayPauseBtn"
                    Text="⏸️ Pause"
                    WidthRequest="120"
                    Clicked="OnPlayPauseClicked" />
            
            <Button x:Name="RestartBtn"
                    Text="🔄 Restart"
                    WidthRequest="120"
                    Clicked="OnRestartClicked" />
        </HorizontalStackLayout>

        <Label Grid.Row="3"
               Text="فایل JSON انیمیشن Lottie را در پوشه Resources/Raw قرار دهید"
               HorizontalOptions="Center"
               FontSize="12"
               TextColor="Gray"
               Margin="0,20,0,0" />
    </Grid>

</ContentPage>
